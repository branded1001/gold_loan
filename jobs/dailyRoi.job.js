const Loan = require("../models/Loan");

const generateDailyROI = async () => {
  // Logic to generate daily ROI for loans
  console.log("Generating daily ROI...");
  // Example: update loans with interest
};

module.exports = { generateDailyROI };